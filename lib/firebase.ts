// Public import alias: `import { auth } from "@/lib/firebase"`
// The actual initialization lives in lib/firebase/config.ts (hot-reload safe).
export { app, auth, storage } from "@/lib/firebase/config";
